{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Puzzle",
            "type": "node",
            "request": "launch",
            "runtimeExecutable": "${workspaceRoot}/node_modules/.bin/jest",
            "program": "${file}",
            "console": "integratedTerminal"
        }
    ]
}